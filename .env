# ForgeMind X — Environment Configuration

# Database (PostgreSQL) Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=forgemind
DB_USER=postgres
DB_PASSWORD=root

# Cache (Redis) Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Backend Settings
SPRING_PROFILES_ACTIVE=local
PORT=8080

# Frontend Settings
VITE_API_BASE_URL=http://localhost:8080
PORT_FRONTEND=5173
